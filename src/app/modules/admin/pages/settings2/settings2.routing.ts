import { Route } from '@angular/router';
import { Settings2Component } from './settings2.component';

export const settings2Routes: Route[] = [
    {
        path: 'settings',
        component: Settings2Component,
    },
];
